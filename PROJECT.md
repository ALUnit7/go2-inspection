# GO2 多模态巡检系统 — 项目总览

> 赛事：2026 睿抗机器人开发者大赛 · CAIR 强体赛道 · 足式机器人 · 多模态巡检  
> 状态：骨架已搭建，各状态触发逻辑待实现  
> 更新：2026-05-29

---

## 硬件环境

| 项目 | 值 |
|------|----|
| 机器人 | Unitree GO2 EDU（固件 ≥ V1.1.6，运控服务名 `mcf`） |
| 拓展坞 | Jetson Orin NX 16GB（IP: 192.168.123.18，上限即为规则算力上限） |
| 开发机 | Ubuntu 22.04，网卡 `enp108s0`，IP: 192.168.123.200 |
| Go2 内置电脑 | IP: 192.168.123.161 |
| 机械臂 | 第三方机械臂（非D1，**协议文档待收**，末端爪型结构） |
| 摄像头 | GO2 前置摄像头 1280×720 15fps（SDK VideoClient 直接读图） |
| 可选外加 | 最多2个外部摄像头 + 1个雷达 + 红外/测距传感器（数量不限） |

**重要约束：比赛时脱线运行，不能有线/无线连接电脑，遥控器放在启停区附近不操作。**

---

## 比赛场地

- 场地尺寸：约 5000×4000mm
- 黑色导引线宽度：约 100mm
- 区域：启停区 → 起点障碍 → 避障区 → 台阶（3级）→ 抓取平台 → 中转平台 → 检测平台 → 放置平台（1号/2号）→ 终点障碍 → 启停区

---

## 比赛物资（需自制，各1个）

| 编号 | 形状 | 尺寸要求 |
|------|------|----------|
| 1号 | 球或半球 | 直径 ≥ 3cm |
| 2号 | 长方体 | 每边 ≥ 3cm |
| 3号 | 正三棱锥 | 每边 ≥ 3cm |
| 4号 | 直圆柱体 | 底面直径&柱高 ≥ 3cm |

物资上可贴标签（数字/ArucoTag），尺寸 ≤ 物资任一表面。

---

## 比赛流程（完整任务序列）

```
启停区
 │
 ▼ 沿黑色导引线行走
[1] 起点障碍  ──── FrontJump 跳过，不碰触 → +10分
 │
 ▼ 沿导引线
[2] 避障区    ──── 从入口进、出口出，不碰挡板 → +20分
 │
 ▼
[3] 台阶（3级）── FreeWalk步态上下，足端触第2级→+20，触最高级→+30
              ── 可在第2级台阶正面贴 10×10cm ArucoTag 辅助定位
 │
 ▼
[4] 抓取平台  ──── 抓取"起始物资"（抽签决定，4选1）→ +10分
              ──── 识别平台正面的"识别标志"（1号/2号标识，抽签决定）
              ──── 记录识别结果，决定最终放到哪个放置平台
 │
 ▼
[5] 中转平台  ──── 将起始物资卸载到中转平台 → +20分
              ──── 从中转平台抓取"场地物资"（另一个抽签物资）→ +10分
 │
 ▼
[6] 检测平台  ──── 机器人垂直投影完全覆盖检测点，停留
              ──── 识别"警示标志"（3选1，抽签），执行对应动作 → +20分
              ──── 当心触电 → Stretch()（伸懒腰）
              ──── 当心强氧化物 → Hello()（打招呼）
              ──── 当心辐射 → 闪烁前灯三次（VuiClient）
 │
 ▼
[7] 放置平台  ──── 根据[4]识别的标志，将场地物资放到对应平台 → +30分
              ──── 1号标识 → 一号放置平台
              ──── 2号标识 → 二号放置平台
 │
 ▼
[8] 终点障碍  ──── FrontJump 跳过，不碰触 → +10分
 │
 ▼
[9] 启停区    ──── 四足完全停在蓝色区域内 → +10分
```

**总分上限（现场任务）：170分 + 报告材料30分 = 200分**

---

## 评分细则

| 项目 | 分值 | 条件 |
|------|------|------|
| 报告材料 | 30 | 赛前一周提交 |
| 起点障碍 | 10 | 跳过且不碰触 |
| 避障区 | 20 | 入口进出口出，不碰挡板 |
| 台阶（2级） | 20 | 足端触第2级或最高级台阶顶面 |
| 台阶（最高级） | 30 | 足端触最高级台阶顶面（与上项取最高） |
| 抓取平台 | 10 | 起始物资离开平台顶面 |
| 中转平台-卸载 | 20 | 起始物资稳定停在中转平台 |
| 中转平台-抓取 | 10 | 场地物资离开中转平台 |
| 检测平台 | 20 | 覆盖检测点+正确动作 |
| 放置平台 | 30 | 场地物资放到正确平台 |
| 终点障碍 | 10 | 跳过且不碰触 |
| 终点停靠 | 10 | 四足完全在蓝色区域 |
| **合计** | **200** | |

违规：人工干预一次 -30分（限10秒，计时不停）

---

## 抽签随机项（运行时决策）

| 抽签 | 内容 | 影响 |
|------|------|------|
| 第1次 | 4选2：起始物资 + 场地物资 | 机械臂抓取目标形状 |
| 第2次 | 2选1：识别标志（1号/2号） | 最终放置平台选择 |
| 第3次 | 3选1：警示标志 | 检测平台执行动作 |

**→ 程序需要在运行时通过视觉识别动态决策，不能硬编码。**

---

## 状态机设计

### 状态 ID 与触发条件

| ID | 状态 | 进入条件 | 退出条件 |
|----|------|----------|----------|
| 0 | IDLE | 程序启动 | 裁判信号（当前：直接启动） |
| 1 | LINE_FOLLOW | 上一任务完成 | 检测到下一区域标记 |
| 2 | JUMP | 检测到障碍物（视觉/固定距离） | 跳跃完成（IMU稳定） |
| 3 | OBSTACLE_AVOID | 进入避障区 | 检测到出口 |
| 4 | CLIMB_STAIRS | 到达台阶前 | IMU pitch恢复平稳 |
| 5 | GRAB_INITIAL | 到达抓取平台 | 起始物资抓取完成 |
| 6 | RECOGNIZE_MARKER | 抓取完成后 | 识别标志读取完成（记录1/2号） |
| 7 | TRANSFER_PLATFORM | 到达中转平台 | 起始物资卸载+场地物资抓取完成 |
| 8 | DETECTION_POINT | 到达检测平台 | 警示动作执行完成 |
| 9 | PLACE_PLATFORM | 到达放置平台 | 场地物资卸载完成 |
| 10 | JUMP_END | 到达终点障碍 | 跳跃完成 |
| 11 | RETURN_HOME | 跳过终点障碍 | 四足停在启停区 |
| 99 | DONE | 停靠完成 | — |

> 当前骨架中 JUMP/OBSTACLE_AVOID/CLIMB_STAIRS/VISION_TASK/ARM_CONTROL 对应上表，
> 需拆分 ARM_CONTROL 为 GRAB_INITIAL / TRANSFER_PLATFORM / PLACE_PLATFORM 三个状态。

### 全局运行时状态（需跨状态传递）

```cpp
struct MissionMemory {
    int  marker_id;        // 抓取平台识别标志：1 或 2
    int  warning_sign;     // 警示标志：0=触电 1=强氧化物 2=辐射
    bool initial_grabbed;  // 起始物资是否在机器人上
    bool field_grabbed;    // 场地物资是否在机器人上
};
```

---

## 视觉识别任务清单

| 识别目标 | 位置 | 方法 | 输出 |
|----------|------|------|------|
| 黑色导引线 | 地面 | 图像二值化+轮廓重心 | 偏差值→转向控制 |
| ArucoTag（台阶） | 第2级台阶正面 | OpenCV ArUco | 距离/角度→定位 |
| 识别标志（1号/2号） | 抓取平台正面 | 图像分类/ArUco | marker_id=1或2 |
| 警示标志 | 检测平台 | 图像分类（3类） | warning_sign=0/1/2 |
| 物资形状（可选） | 各平台 | YOLO目标检测 | 物资类别 |

---

## 检测平台动作映射

| 警示标志 | SDK 调用 |
|----------|----------|
| 当心触电 | `sport_client.Stretch()` |
| 当心强氧化物 | `sport_client.Hello()` |
| 当心辐射 | `vui_client.SetLedEnable(true)` × 3次闪烁 |

---

## 项目目录结构

```
GO2_RCOM/
├── CMakeLists.txt
├── config/mission.yaml          # 运行时参数
├── src/
│   ├── main.cpp
│   ├── core/
│   │   ├── StateBase.hpp        # enter/update/exit 基类
│   │   ├── StateIDs.hpp         # 状态枚举
│   │   ├── RobotContext.hpp     # 共享上下文（SDK客户端+配置+MissionMemory）
│   │   └── MissionRunner        # 50Hz 状态机驱动
│   ├── states/                  # 每个任务一个状态（待按新流程拆分）
│   │   ├── StateIdle
│   │   ├── StateLineFollow      # 黑线寻迹
│   │   ├── StateJump            # 跳跃障碍（起点/终点复用）
│   │   ├── StateObstacleAvoid   # 避障区
│   │   ├── StateClimbStairs     # 三级台阶
│   │   ├── StateGrabInitial     # 抓取起始物资 + 识别标志
│   │   ├── StateTransfer        # 中转平台（卸载+抓取场地物资）
│   │   ├── StateDetectionPoint  # 检测平台（识别警示+执行动作）
│   │   ├── StatePlacePlatform   # 放置平台（根据marker_id选1/2号）
│   │   └── StateReturnHome      # 终点停靠
│   ├── robot/
│   │   ├── SportController      # SportClient 封装
│   │   └── StateMonitor         # rt/sportmodestate 订阅
│   ├── perception/
│   │   ├── CameraClient         # VideoClient → cv::Mat
│   │   ├── LineDetector         # 黑线检测，返回偏差
│   │   ├── ArucoDetector        # ArUco标签检测（台阶定位+识别标志）
│   │   ├── SignClassifier       # 警示标志分类（3类）
│   │   └── Detector             # YOLO（物资形状，可选）
│   └── arm/
│       └── ArmInterface         # 机械臂抽象层 + ArmStub 桩
├── models/                      # .onnx 模型（不入git）
├── scripts/
│   ├── build.sh
│   ├── deploy_jetson.sh
│   └── run_mission.sh
└── docs/
    └── 4dce5ce3b0004d4bb86889a57ff5e171.pdf  # 比赛手册原文
```

---

## 待实现（优先级排序）

### P0 — 必须完成（直接得分）
- [ ] **黑线寻迹** — 100mm黑线，地面颜色待确认，需现场标定阈值
- [ ] **跳跃障碍** — 起点+终点各一次，触发时机（视觉检测障碍物距离）
- [ ] **台阶通过** — ArucoTag辅助定位，FreeWalk步态，IMU判断完成
- [ ] **检测平台停靠** — 精确停靠使投影覆盖检测点
- [ ] **警示标志识别** — 3类图像分类（触电/强氧化物/辐射）
- [ ] **警示动作执行** — Stretch / Hello / VuiClient灯光

### P1 — 高分项
- [ ] **识别标志读取** — 抓取平台正面1号/2号标识识别
- [ ] **机械臂协议** — D1臂通信实现（替换ArmStub）
- [ ] **抓取起始物资** — 抓取平台操作
- [ ] **中转平台操作** — 卸载+抓取
- [ ] **放置平台选择** — 根据marker_id动态选择1/2号平台

### P2 — 完整流程
- [ ] **MissionMemory 跨状态传递** — marker_id / warning_sign
- [ ] **状态机重构** — 按新流程拆分ARM_CONTROL为3个状态
- [ ] **终点停靠** — 精确停在蓝色区域
- [ ] **VuiClient灯光控制** — 辐射标志对应动作

### P3 — 鲁棒性
- [ ] **丢线恢复** — 连续丢线后的恢复策略
- [ ] **人工干预后恢复** — 从任意位置重新定位
- [ ] **参数现场标定** — 线颜色阈值、速度、距离阈值

---

## 关键参数（config/mission.yaml）

| 参数 | 当前值 | 说明 |
|------|--------|------|
| `network.interface` | `enp108s0` | 连接 Go2 的网卡（Jetson上改为对应网卡） |
| `tasks.line_follow.speed` | `0.3 m/s` | 寻迹速度 |
| `tasks.line_follow.kp` | `0.005` | 偏差→转向比例系数 |
| `tasks.jump.approach_speed` | `0.5 m/s` | 接近障碍速度 |
| `tasks.climb_stairs.speed` | `0.2 m/s` | 爬台阶速度 |
| `tasks.vision.conf_thresh` | `0.5` | 检测置信度阈值 |

---

## 构建与部署

```bash
# 本地构建（开发机）
./scripts/build.sh

# 部署到 Jetson 并远程编译
./scripts/deploy_jetson.sh [192.168.123.18]

# 运行（Jetson上，脱线模式）
sudo ./build/go2_inspection config/mission.yaml
```

---

## 开放问题（需确认）

1. **机械臂协议** — 第三方机械臂文档待收，收到后实现 ArmInterface
2. **识别标志读取** — 其他成员负责，代码待整合
3. **黑线寻迹** — 其他成员负责，代码待整合
4. **场地地面颜色** — 白色/灰色/其他？影响黑线二值化阈值
5. **障碍物高度** — 起点/终点障碍物规格（影响跳跃时机判断）
6. **台阶规格** — 每级高度/深度（影响爬台阶速度参数）
7. **各平台高度/位置** — 抓取/中转/放置平台的精确尺寸
8. **Jetson 网卡名** — 部署时需更新 config/mission.yaml
